extends base

block content
    h4 Update Video 
    form(method="POST")
        input(name="title",placeholder="Video Title", value=video.title, required)
        input(placeholder="Description",required, type="text", name ="discription" minlength=20, value = video.description)
        input(placeholder="Hashtags,separated by comma.",required, type="text", name ="hashtags", value = video.hashtags.join()) 
        input(value="Save", type="submit")

        //- join()은 뭐지
        //- join()은 JavaScript의 배열 메서드입니다. 
        //- 이 메서드는 배열의 모든 요소를 연결하여 하나의 문자열을 생성합니다. 
        //- 기본적으로 각 요소 사이에 , (콤마)를 삽입하여 연결하지만, 
        //- 다른 구분자를 지정할 수도 있습니다.